<svg
    width="{{ default 24 .size }}"
    height="{{ default 24 .size }}"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    class="i-check {{ .class }}"
    {{ if .hidden }}aria-hidden="true"{{ end }}
>
  <polyline points="20 6 9 17 4 12"></polyline>
</svg>
