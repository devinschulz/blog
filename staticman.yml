comments:
  allowedFields: ["name", "email", "body", "website", "parent"]
  allowedOrigins: ["localhost", "devinschulz.com"]
  branch: "master"
  generatedFields:
    date:
      type: date
  format: "yml"
  moderation: true
  name: "Devin Schulz"
  path: "data/comments/{options.entryId}"
  filename: "{@id}"
  requiredFields: ["name", "body"]
  transforms:
    email: md5
