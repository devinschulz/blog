[build]
  command = "NODE_ENV=production hugo --gc --minify"  
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.64.1"
  NPM_VERSION = "6.13.7"
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_ENV = "production"

[context.deploy-preview]
  command = "NODE_ENV=production hugo --gc --minify --buildFuture"

[context.deploy-preview.environment]
  HUGO_ENV = "preview"
